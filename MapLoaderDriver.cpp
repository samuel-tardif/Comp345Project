#include <iostream>;
#include <fstream>;
#include <string>;
#include <vector>;
#include "MapLoader.h"
using namespace std;

int main() {
	MapLoader test;
	test.readFile("test.txt");

	//Graph graph(edges, N);
	//Graph printGraph(graph, N);
}
